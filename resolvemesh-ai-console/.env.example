## Frontend → PDF FastAPI service
# If your PDF service runs on port 8000:
# VITE_PDF_SERVICE_URL=http://localhost:8000
#
# If port 8000 is already used (e.g. by backend/main.py), run the PDF service on a different port:
# VITE_PDF_SERVICE_URL=http://localhost:8001

